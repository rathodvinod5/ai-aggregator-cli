# Dependency directories (never commit these, they are too large)
node_modules/

# Local environment variables containing your secret keys (CRITICAL)
.env
.env.local
.env.production

# Log files generated by errors
*.log
npm-debug.log*

# System files (macOS specific)
.DS_Store
